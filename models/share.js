var Share = function(swipe, sharePartnerId, config) {
	this.swipe = swipe;
	this.sharePartnerId = sharePartnerId;
	this.config = config;
};

Share.prototype.swipe;
Share.prototype.sharePartnerId;
Share.prototype.config;

module.exports = Share;
